func processContinue(arr [10]int) int {
            var sum = 0;
            for i := 0; i < 10; i := i + 1 {
                if (arr[i] < 0) {
                    continue;
                }
                sum := sum + arr[i];
            }
            return sum;
        };