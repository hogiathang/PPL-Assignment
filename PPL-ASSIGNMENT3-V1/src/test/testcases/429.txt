
        var v AAAA;      
        type AAAA struct {
            a int;
        } 
        type cccc interface {
            foo() int;
            akazam() int;
        }
        func (z AAAA) akazam(a int, b float) int {return 1;}
        func (b AAAA) koo(a int) {b.koo(a);}
        func foo() {
            var x cccc;
            const c = x.akazam(1);  
        }
        